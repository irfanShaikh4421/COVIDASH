import React from 'react';

const Statistics = (props) => {
    return (
        <p>
            This is a placeholder page for the statistics of Covid cases, both regionally and globally.
        </p>
    );
}

export default Statistics;